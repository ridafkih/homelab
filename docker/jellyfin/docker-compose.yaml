services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    volumes:
      - .jellyfin/config:/config
      - .jellyfin/cache:/cache
      - /data/media/:/media:ro
    restart: always
    runtime: nvidia
    env_file:
      - .env
    networks:
      - homelab-network

networks:
  homelab-network:
    external: true
